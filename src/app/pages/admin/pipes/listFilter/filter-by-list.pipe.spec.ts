import { FilterByListPipe } from './filter-by-list.pipe';

describe('FilterByListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByListPipe();
    expect(pipe).toBeTruthy();
  });
});
